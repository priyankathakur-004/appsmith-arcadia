SELECT DISTINCT
    aca.IdClient AS id_client,
    c.ClientName AS client_name
FROM arcadia.ArcadiaCredentialAccount aca
INNER JOIN dbo.Client c ON c.IdClient = aca.IdClient
ORDER BY c.ClientName, aca.IdClient;